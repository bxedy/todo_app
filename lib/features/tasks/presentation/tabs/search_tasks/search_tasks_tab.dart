import 'package:flutter/material.dart';

class SearchTasksTab extends StatelessWidget {
  const SearchTasksTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('SearchTasksTab'));
  }
}